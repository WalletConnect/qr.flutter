name: example
description: qr_flutter_wc example app.
version: 3.0.0
homepage: https://github.com/WalletConnect/qr.flutter
publish_to: none

environment:
  sdk: '>=2.19.6 <3.0.0'
  flutter: ">=3.7.0"

dependencies:
  flutter:
    sdk: flutter
  qr_flutter_wc:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: false
  assets:
    - assets/logo_wc.png
